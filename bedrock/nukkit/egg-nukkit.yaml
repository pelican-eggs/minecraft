_comment: 'DO NOT EDIT: FILE GENERATED AUTOMATICALLY BY PANEL'
meta:
  version: PLCN_v3
  update_url: 'https://raw.githubusercontent.com/pelican-eggs/minecraft/refs/heads/main/bedrock/nukkit/egg-nukkit.yaml'
exported_at: '2026-06-07T18:08:23+00:00'
name: Nukkit
author: parker@parkervcp.com
uuid: 5669130a-d4a2-4a80-88b6-9edd2d9484bc
description: "Nukkit is a Nuclear-Powered Server Software For Minecraft: Pocket Edition\r\n\r\nhttps://cloudburstmc.org"
tags:
  - minecraft
features: null
docker_images:
  'ghcr.io/pelican-eggs/yolks:java_11': 'ghcr.io/pelican-eggs/yolks:java_11'
  'ghcr.io/pelican-eggs/yolks:java_16': 'ghcr.io/pelican-eggs/yolks:java_16'
  'ghcr.io/pelican-eggs/yolks:java_8': 'ghcr.io/pelican-eggs/yolks:java_8'
  'ghcr.io/pelican-eggs/yolks:java_17': 'ghcr.io/pelican-eggs/yolks:java_17'
file_denylist: {  }
config:
  files:
    server.properties:
      parser: properties
      find:
        server-ip: 0.0.0.0
        enable-query: true
        server-port: '{{server.allocations.default.port}}'
        query.port: '{{server.allocations.default.port}}'
  startup:
    done: ')! '
  logs: {  }
  stop: stop
scripts:
  installation:
    script: |-
      #!/bin/ash
      # Nukkit Installation Script
      #
      # Server Files: /mnt/server
      apk add --no-cache curl openssl

      cd /mnt/server

      ## Only download if a path is provided, otherwise continue.
      if [ ! -z "${DL_PATH}" ]; then
          MODIFIED_DOWNLOAD=`eval echo $(echo ${DL_PATH} | sed -e 's/{{/${/g' -e 's/}}/}/g')`
          wget ${MODIFIED_DOWNLOAD} -O ${SERVER_JARFILE}
      elif [ -z "${NUKKIT_VERSION}" ] || [ "${NUKKIT_VERSION}" == "latest" ]; then
          wget https://ci.opencollab.dev/job/NukkitX/job/Nukkit/job/master/lastSuccessfulBuild/artifact/target/nukkit-1.0-SNAPSHOT.jar -O ${SERVER_JARFILE}
      else
          wget https://ci.opencollab.dev/job/NukkitX/job/Nukkit/job/master/${NUKKIT_VERSION}/artifact/target/nukkit-1.0-SNAPSHOT.jar -O ${SERVER_JARFILE}
      fi

      if [ ! -f nukkit.yml ]; then
          wget https://raw.githubusercontent.com/pelican-eggs/minecraft/main/bedrock/nukkit/nukkit.yml
      fi

      if [ ! -f server.properties ]; then
          wget https://raw.githubusercontent.com/pelican-eggs/minecraft/main/bedrock/nukkit/server.properties
      fi

      echo -s "Install completed!"
    container: 'ghcr.io/pelican-eggs/installers:alpine'
    entrypoint: ash
variables:
  -
    name: 'Server Jar File'
    description: 'The name of the server jarfile to run the server with.'
    env_variable: SERVER_JARFILE
    default_value: server.jar
    user_viewable: true
    user_editable: true
    rules:
      - required
      - 'regex:/^([\w\d._-]+)(\.jar)$/'
    sort: null
  -
    name: 'Download Path'
    description: |-
      A URL to use to download Nukkit rather than building it on the server. This is not user viewable.
      Use <code>{{DL_VERSION}}</code> in the URL to automatically insert the assigned version into the
      URL. If you do not enter a URL Spigot will build directly in the container (this will fail on low
      memory containers).
    env_variable: DL_PATH
    default_value: ''
    user_viewable: false
    user_editable: false
    rules:
      - nullable
      - string
    sort: null
  -
    name: 'nukkit version'
    description: 'The version of Nukkitt to download (using the --rev tag). Use "latest" for latest.'
    env_variable: NUKKIT_VERSION
    default_value: latest
    user_viewable: true
    user_editable: true
    rules:
      - required
      - string
      - 'max:20'
    sort: null
startup_commands:
  Default: 'java -Xms128M -Xmx{{SERVER_MEMORY}}M -jar {{SERVER_JARFILE}}'
