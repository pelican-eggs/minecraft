_comment: 'DO NOT EDIT: FILE GENERATED AUTOMATICALLY BY PANEL'
meta:
  version: PLCN_v3
  update_url: 'https://raw.githubusercontent.com/pelican-eggs/minecraft/refs/heads/main/java/glowstone/egg-glowstone.yaml'
exported_at: '2026-06-07T18:08:32+00:00'
name: Glowstone
author: support@pterodactyl.io
uuid: 0b0b94ea-aff2-49c0-80a0-736c1717f82e
description: 'Glowstone is an open-source server implementation for Minecraft: Java Edition 1.12.2 and up.'
image: null
tags:
  - minecraft
features: null
docker_images:
  'Java 8': 'ghcr.io/pelican-eggs/yolks:java_8'
  'Java 11': 'ghcr.io/pelican-eggs/yolks:java_11'
  'Java 16': 'ghcr.io/pelican-eggs/yolks:java_16'
  'Java 17': 'ghcr.io/pelican-eggs/yolks:java_17'
  'Java 18': 'ghcr.io/pelican-eggs/yolks:java_18'
file_denylist: {  }
startup_commands:
  Default: 'java -Xms768M -Xmx{{SERVER_MEMORY}}M -XX:+UseG1GC -jar {{SERVER_JARFILE}}'
config:
  files:
    config/glowstone.yml:
      parser: yaml
      find:
        server.port: '{{server.allocations.default.port}}'
        console.prompt: ''
  startup:
    done: 'Ready for connections.'
  logs:
    custom: false
    location: logs/latest.log
  stop: stop
scripts:
  installation:
    script: |-
      #!/bin/bash
      # Server Files: /mnt/server
      apt update
      apt install -y jq curl

      mkdir -p /mnt/server
      cd /mnt/server

      if [ $VERSION == *SNAPSHOT ]; then
        echo -e "Using Maven"
        
        DOWNLOAD_URL="https://repo.glowstone.net/service/rest/v1/search/assets/download?group=net.glowstone&name=glowstone&sort=version&repository=snapshots&maven.baseVersion=${VERSION}&direction=desc&maven.classifier="
      else 
          echo -e "Using GitHub."

          if [ -z "${GITHUB_USER}" ] && [ -z "${GITHUB_OAUTH_TOKEN}" ] ; then
              echo -e "using anon api call"
          else
              echo -e "user and oauth token set"
              alias curl='curl -u ${GITHUB_USER}:${GITHUB_OAUTH_TOKEN} '
          fi
          
          ## get release info and download links
          LATEST_JSON=$(curl --silent "https://api.github.com/repos/${GITHUB_PACKAGE}/releases/latest")
          RELEASES=$(curl --silent "https://api.github.com/repos/${GITHUB_PACKAGE}/releases")

          if [ -z "${VERSION}" ] || [ "${VERSION}" == "latest" ]; then
              DOWNLOAD_URL=$(echo ${LATEST_JSON} | jq .assets | jq -r .[].browser_download_url | grep -i ${MATCH})
          else
              VERSION_CHECK=$(echo ${RELEASES} | jq -r --arg VERSION "${VERSION}" '.[] | select(.tag_name==$VERSION) | .tag_name')
              if [ "${VERSION}" == "${VERSION_CHECK}" ]; then
                  DOWNLOAD_URL=$(echo ${RELEASES} | jq -r --arg VERSION "${VERSION}" '.[] | select(.tag_name==$VERSION) | .assets[].browser_download_url' | grep -i ${MATCH})
              else
                  echo -e "defaulting to latest release"
                  DOWNLOAD_URL=$(echo ${LATEST_JSON} | jq .assets | jq -r .[].browser_download_url)
              fi
          fi
      fi

      echo -e "Downloading..."
      curl --silent -L -o ${SERVER_JARFILE} ${DOWNLOAD_URL}

      echo -e "Generating configuration..."
      java -jar ${SERVER_JARFILE} --generate-config

      echo -e "Install Complete"
    container: 'ghcr.io/pelican-eggs/installers:java_8'
    entrypoint: bash
variables:
  -
    name: 'GitHub OAuth Token'
    description: "This can be either an OAuth or a Personal Access Token.\r\n\r\nThis is required for the install is you set a user."
    env_variable: GITHUB_OAUTH_TOKEN
    default_value: ''
    user_viewable: false
    user_editable: false
    rules:
      - string
      - nullable
    sort: null
  -
    name: 'GitHub Package'
    description: 'The repository to download from.'
    env_variable: GITHUB_PACKAGE
    default_value: GlowstoneMC/Glowstone
    user_viewable: true
    user_editable: false
    rules:
      - required
      - string
    sort: null
  -
    name: 'GitHub User'
    description: "GitHub user to use for api calls.\r\n\r\nThis only needs to be set if you hit the GitHub API too often across multiple servers."
    env_variable: GITHUB_USER
    default_value: ''
    user_viewable: false
    user_editable: false
    rules:
      - string
      - nullable
    sort: null
  -
    name: 'Glowstone Version'
    description: |-
      The version of Glowstone to download. If it ends in SNAPSHOT, it will attempt to download from the
      maven repository.
    env_variable: VERSION
    default_value: latest
    user_viewable: true
    user_editable: true
    rules:
      - required
      - string
      - 'max:20'
    sort: null
  -
    name: Match
    description: ''
    env_variable: MATCH
    default_value: glowstone.jar
    user_viewable: false
    user_editable: false
    rules:
      - required
      - string
    sort: null
  -
    name: 'Server Jar File'
    description: 'The name of the server jarfile to run the server with.'
    env_variable: SERVER_JARFILE
    default_value: glowstone.jar
    user_viewable: true
    user_editable: true
    rules:
      - required
      - 'regex:/^([\w\d._-]+)(\.jar)$/'
    sort: null
