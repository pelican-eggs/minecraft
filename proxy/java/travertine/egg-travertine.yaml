_comment: 'DO NOT EDIT: FILE GENERATED AUTOMATICALLY BY PANEL'
meta:
  version: PLCN_v3
  update_url: 'https://raw.githubusercontent.com/pelican-eggs/minecraft/refs/heads/main/proxy/java/travertine/egg-travertine.yaml'
exported_at: '2026-01-06T14:27:14+00:00'
name: Travertine
author: parker@parkervcp.com
uuid: fce95d08-d726-438c-a1d3-28798071bd91
description: |-
  Travertine is a fork of Waterfall with 1.7 protocol support. Waterfall is a fork of the well-known
  BungeeCord server teleportation suite.
image: null
tags: {  }
features:
  - eula
  - java_version
  - pid_limit
docker_images:
  Java_17: 'ghcr.io/pelican-eggs/yolks:java_17'
  Java_16: 'ghcr.io/pelican-eggs/yolks:java_16'
  Java_11: 'ghcr.io/pelican-eggs/yolks:java_11'
  Java_8: 'ghcr.io/pelican-eggs/yolks:java_8'
file_denylist: {  }
startup_commands:
  Default: 'java -Xms128M -XX:MaxRAMPercentage=95.0 -Dterminal.jline=false -Dterminal.ansi=true -jar {{SERVER_JARFILE}}'
config:
  files:
    config.yml:
      parser: yaml
      find:
        'listeners[0].host': '0.0.0.0:{{server.allocations.default.port}}'
        'servers.*.address':
          127.0.0.1: '{{config.docker.interface}}'
          localhost: '{{config.docker.interface}}'
  startup:
    done: 'Listening on '
  logs: {  }
  stop: end
scripts:
  installation:
    script: |-
      #!/bin/bash
      # Travertine Installation Script
      #
      # Server Files: /mnt/server
      PROJECT=travertine

      if [ -n "${DL_PATH}" ]; then
      	echo -e "Using supplied download url: ${DL_PATH}"
      	DOWNLOAD_URL=`eval echo $(echo ${DL_PATH} | sed -e 's/{{/${/g' -e 's/}}/}/g')`
      else
      	VER_EXISTS=`curl -s https://papermc.io/api/v2/projects/${PROJECT} | jq -r --arg VERSION $MINECRAFT_VERSION '.versions[] | contains($VERSION)' | grep true`
      	LATEST_VERSION=`curl -s https://papermc.io/api/v2/projects/${PROJECT} | jq -r '.versions' | jq -r '.[-1]'`

      	if [ "${VER_EXISTS}" == "true" ]; then
      		echo -e "Version is valid. Using version ${MINECRAFT_VERSION}"
      	else
      		echo -e "Using the latest ${PROJECT} version"
      		MINECRAFT_VERSION=${LATEST_VERSION}
      	fi
      	
      	BUILD_EXISTS=`curl -s https://papermc.io/api/v2/projects/${PROJECT}/versions/${MINECRAFT_VERSION} | jq -r --arg BUILD ${BUILD_NUMBER} '.builds[] | tostring | contains($BUILD)' | grep true`
      	LATEST_BUILD=`curl -s https://papermc.io/api/v2/projects/${PROJECT}/versions/${MINECRAFT_VERSION} | jq -r '.builds' | jq -r '.[-1]'`
      	
      	if [ "${BUILD_EXISTS}" == "true" ]; then
      		echo -e "Build is valid for version ${MINECRAFT_VERSION}. Using build ${BUILD_NUMBER}"
      	else
      		echo -e "Using the latest ${PROJECT} build for version ${MINECRAFT_VERSION}"
      		BUILD_NUMBER=${LATEST_BUILD}
      	fi
      	
      	JAR_NAME=${PROJECT}-${MINECRAFT_VERSION}-${BUILD_NUMBER}.jar
      	
      	echo "Version being downloaded"
      	echo -e "MC Version: ${MINECRAFT_VERSION}"
      	echo -e "Build: ${BUILD_NUMBER}"
      	echo -e "JAR Name of Build: ${JAR_NAME}"
      	DOWNLOAD_URL=https://papermc.io/api/v2/projects/${PROJECT}/versions/${MINECRAFT_VERSION}/builds/${BUILD_NUMBER}/downloads/${JAR_NAME}
      fi

      cd /mnt/server

      echo -e "Running curl -o ${SERVER_JARFILE} ${DOWNLOAD_URL}"

      if [ -f ${SERVER_JARFILE} ]; then
      	mv ${SERVER_JARFILE} ${SERVER_JARFILE}.old
      fi

      curl -o ${SERVER_JARFILE} ${DOWNLOAD_URL}


      ## install end
      echo "-----------------------------------------"
      echo "Installation completed..."
      echo "-----------------------------------------"
    container: 'ghcr.io/pelican-eggs/installers:debian'
    entrypoint: bash
variables:
  -
    sort: 4
    name: 'Travertine build number'
    description: "Default is latest.\r\n\r\nif set to latest or an invalid version will grab the latest build number."
    env_variable: BUILD_NUMBER
    default_value: latest
    user_viewable: true
    user_editable: true
    rules:
      - required
      - string
      - 'max:20'
  -
    sort: 3
    name: 'Download Link'
    description: 'A link to the server jar to download the Travertine jar.'
    env_variable: DL_LINK
    default_value: ''
    user_viewable: true
    user_editable: true
    rules:
      - nullable
      - string
  -
    sort: 1
    name: 'Minecraft Version'
    description: 'The version of Minecraft that water was built to support.'
    env_variable: MINECRAFT_VERSION
    default_value: latest
    user_viewable: true
    user_editable: true
    rules:
      - required
      - alpha_num
      - 'between:1,6'
  -
    sort: 2
    name: 'Travertine Jar File'
    description: 'The name of the jar file to use when running Travertine.'
    env_variable: SERVER_JARFILE
    default_value: travertine.jar
    user_viewable: true
    user_editable: true
    rules:
      - required
      - 'regex:/^([\w\d._-]+)(\.jar)$/'
